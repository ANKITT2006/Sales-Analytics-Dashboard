import { type SalesRecord } from "@/types/sales";

/**
 * Live Indian Retail Sales Collection
 * All mock/synthetic values purged. Starts as an empty collection awaiting live verified transactions.
 */
export const SALES_DATA: SalesRecord[] = [];

/** Default monthly sales target in INR (₹50,000) */
export const MONTHLY_SALES_TARGET = 50_000;
